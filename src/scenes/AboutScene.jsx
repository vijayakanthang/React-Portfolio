/**
 * About scene — intentionally empty. The About section is now a pure DOM
 * layout (identity + story); no 3D avatar or geometry sits behind it.
 * Kept as a placeholder so the scene list / bands stay aligned.
 */
export default function AboutScene() {
  return null;
}
